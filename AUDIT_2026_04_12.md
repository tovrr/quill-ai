# Quill AI — Comprehensive Project Audit
**Audit Date:** April 12, 2026 | **Auditor:** GitHub Copilot | **Branch:** `main` | **Version:** 1.0.0

---

## Methodology & Evidence Sources

| Source | Coverage |
|---|---|
| Full codebase read (`src/`, `scripts/`, config files) | Architecture, security, data, quality |
| `package.json` dependency manifest | Stack, versions, license surface |
| `TODOS.md`, `REMEDIATION_PLAN_30D.md` | Decision records, known debt |
| `DEPLOYMENT_CHECKLIST.md`, `DEV_RUNBOOK.md` | Ops, incident response patterns |
| `next.config.ts`, `src/lib/`, `src/app/api/` | Security headers, rate limiting, access control |
| `src/db/schema.ts`, `drizzle/` migrations | Data model, lineage |
| `src/lib/chat/`, `src/lib/killers.ts` | AI/ML, prompt governance |
| Repo memory files | Prior decisions, architecture rationale |

**Limitations:** No CI pipeline files observed in the workspace; no load test data; no Vercel dashboard access; no external SAST/pen test reports; no stakeholder interviews performed.

---

## Executive Summary

Quill AI is a well-engineered personal AI agent and builder application built on Next.js 16, React 19, and Google Gemini. The codebase demonstrates strong security discipline (enforced CSP, HSTS, rate limiting, token-safe metrics), a clean modular architecture, and a thoughtful AI tier model. The product reached a genuine v1.0.0 milestone with a working builder, canvas preview, and multi-mode chat.

**However, the product is not yet commercially viable.** Three foundational blockers prevent revenue generation and customer trust: there is no payment processor, no social/OAuth login, and the sandbox execution capability is partially false-claimed. Additionally, observability is ephemeral (in-memory metrics reset on restart), CI/CD pipeline visibility is absent from the workspace, and external uptime monitoring is undeployed. The remaining risk surface is manageable with focused 4–8 week execution.

**Overall Maturity Rating: 6.5 / 10** — Strong technical foundation; pre-revenue; observability and monetization gaps are the principal risks.

---

## Domain 1 — Governance, Artifacts & Decision Records

### Findings

| ID | Severity | Finding |
|---|---|---|
| G-01 | Medium | `TODOS.md` serves as the combined backlog, roadmap, and decision log. This works at current team size but lacks traceability between decisions and implementation commits. |
| G-02 | Low | `AGENTS.md` is a well-structured behavioral contract for AI agents but has no versioning or change history mechanism. |
| G-03 | Low | No ADR (Architecture Decision Record) directory exists; decisions are embedded in markdown TODOs, making historical context difficult to recover. |
| G-04 | Low | `SOUL.md`, `IDENTITY.md`, `USER.md` establish product philosophy and persona but are not linked to any governance or release gate process. |

### Recommendations

- **G-R01 (Owner: Tech Lead | Priority: Low | Effort: 2h):** Create a lightweight `docs/decisions/` ADR directory. Port the 10 most consequential decisions already embedded in TODOs (e.g., auth library choice, CSP two-phase rollout, entitlements model).
- **G-R02 (Owner: Tech Lead | Priority: Low | Effort: 1h):** Add a `CHANGELOG.md` that maps milestone commits to user-visible capability changes.

---

## Domain 2 — Technical Architecture

### Stack Assessment

| Layer | Technology | Version | Risk |
|---|---|---|---|
| Framework | Next.js | 16.1.3 | Low — current major |
| UI runtime | React | 19.2.3 | Low — current |
| Language | TypeScript | (tsconfig strict) | Low |
| AI SDK | Vercel AI SDK | 6.0.141 | Low — current |
| Primary AI | Google Gemini (Flash-Lite / Flash / Pro) | API v1 | Medium — single provider dependency |
| Alt AI | OpenRouter (free model fallback) | — | Low |
| Auth | Better Auth | 1.5.6 | Low — recent version |
| ORM | Drizzle ORM | 0.45.2 | Low |
| Database | Neon Postgres (serverless) | @neondatabase/serverless 1.0.2 | Medium — vendor lock-in |
| Rate limiting | Upstash Redis + in-memory fallback | REST API | Low |
| Web search | Tavily | API key | Low |
| Code execution | Docker (local spawn) | python:3.13-slim | High — see A-03 |
| CSS | Tailwind CSS 4 | 4.2.2 | Low |
| Icons | Heroicons | 2.1.1 | Low |

### Findings

| ID | Severity | Finding |
|---|---|---|
| A-01 | High | **Single AI provider dependency.** Gemini is the sole provider for Think and Pro tiers. No fallback exists if Google's API degrades. The OpenRouter fallback is Fast-mode only and gated behind `OPENROUTER_FAST_ENABLED=true`. |
| A-02 | Medium | **In-memory metrics are volatile.** `src/lib/api-metrics.ts` stores all route metrics in `globalThis`. A Vercel cold start or edge function lifecycle reset silently drops all historical data. No persistent metrics store (Prometheus, DataDog, Posthog, etc.) is connected. |
| A-03 | High | **Docker code executor has no production path.** `src/lib/docker-executor.ts` spawns a local Docker container via `child_process.spawn`. This works on developer machines but is incompatible with Vercel serverless deployments. `QUILL_SANDBOX_CONTAINER_ENABLED=true` is a deployment-time footgun — it will fail silently or crash in production. |
| A-04 | Medium | **React preview runs Babel standalone in the client iframe.** `src/components/agent/CanvasPanel.tsx` loads Babel in the browser for JSX transpilation. This adds ~250KB to the canvas interaction path and could be replaced by a server-side transpile step or a more targeted runtime. |
| A-05 | Low | **Neon Postgres is the sole datastore.** No read replica, no cache layer (Redis is used only for rate limiting). Complex queries against growing `message`/`chat` tables may degrade without index expansion. |
| A-06 | Low | **`src/proxy.ts` exists but appears unused by default.** Verify whether this is a development artifact or an intended production path. |

### Recommendations

- **A-R01 (Owner: Backend | Priority: High | Effort: 3d):** Add a thin provider abstraction that routes Think/Pro to a secondary provider (e.g., Anthropic Claude or OpenAI) when Gemini returns 429/503. `THINKING_MODEL_OVERRIDE` partially supports this but doesn't auto-failover.
- **A-R02 (Owner: Infra | Priority: Medium | Effort: 1d):** Replace in-memory metrics with a persistent sink. Minimum viable: push aggregated counters to the Neon DB on a write-behind schedule, or integrate a free tier of PostHog/Axiom.
- **A-R03 (Owner: Backend | Priority: High | Effort: 2d):** Either (a) remove `docker-executor` from the production build and gate it cleanly behind a feature flag with a build-time error if enabled on Vercel, or (b) replace with a cloud code execution service (e.g., E2B, Modal, fly.io microVM). This resolves both the false-claims risk and the production safety issue documented in TODOS.md.

---

## Domain 3 — Security

### Findings

| ID | Severity | Finding | Evidence |
|---|---|---|---|
| S-01 | Medium | **CSP `report-uri` endpoint (`/api/csp-report`) has no ingest, storage, or alerting.** Violations are reported but silently discarded if the endpoint does nothing. | `next.config.ts:37` |
| S-02 | Medium | **`script-src 'unsafe-inline'` is present in the enforced CSP (production).** This weakens XSS protection. Mitigating factor: no `'unsafe-eval'` in prod. Ideal path is CSP nonces via Next.js middleware. | `next.config.ts:25` |
| S-03 | High | **Entitlement bypass via env vars (`PAID_USER_EMAILS`, `PAID_USER_IDS`).** These are comma-separated plaintext lists in environment variables. Any operator with env access can silently grant or deny paid access to any user without an audit trail. Without a payment processor this is currently the only access gate. | `src/lib/entitlements.ts:35-44` |
| S-04 | Low | **`accounts` table stores `accessToken`, `refreshToken`, and `idToken` in plaintext.** For OAuth providers (once Google login is added), these are sensitive fields that should be encrypted at rest at the application layer, or the DB should enforce column encryption. | `src/db/schema.ts:50-68` |
| S-05 | Low | **`sessions` table stores `ipAddress` and `userAgent`.** This is PII under GDPR. No retention policy is documented or enforced. | `src/db/schema.ts:32-40` |
| S-06 | Low | **Tavily API key passed in the `body` of a POST request.** The standard Tavily API accepts `api_key` in the JSON body, but this means the key appears in request logs. Prefer the `Authorization: Bearer` header if the API supports it. | `src/lib/web-search.ts:52` |
| S-07 | Medium | **No `npm audit` gate in deployment or CI.** The `DEPLOYMENT_CHECKLIST.md` lists `npm install` but not `npm audit`. No CVE scanning policy is defined. | `TODOS.md:Audit-Driven Backlog` |
| S-08 | Low | **HSTS `max-age=31536000` is applied on all responses including HTTP.** The HSTS header should only be sent on HTTPS responses; on platforms like Vercel this is handled at the termination layer but could cause issues in custom proxy setups. | `next.config.ts:82` |

### Recommendations

- **S-R01 (Owner: Backend | Priority: High | Effort: 4h):** Implement the `/api/csp-report` endpoint to parse and log (or store) violation reports. Minimum: `console.error` with structured JSON; ideal: persist to DB or forward to a log aggregator.
- **S-R02 (Owner: Backend | Priority: Medium | Effort: 1d):** Replace entitlement env-var overrides with an audited admin UI (`/admin/model-usage` page already exists as a shell — expand its scope) once Stripe is implemented. Until then, document the trust model explicitly.
- **S-R03 (Owner: Backend | Priority: Medium | Effort: 3h):** Add `npm audit --audit-level=high` to the pre-deploy checklist and create a quarterly dependency upgrade issue template.
- **S-R04 (Owner: Backend | Priority: Low | Effort: 2h):** Add a CSP nonce via Next.js middleware to eliminate `'unsafe-inline'` from script-src in the enforced policy.

---

## Domain 4 — Data Management

### Schema Assessment

The data model is clean and minimal:
- `user`, `session`, `account`, `verification` — Better Auth standard tables
- `chat` — per-user conversation records with title, sharing flag, creation timestamps
- `message` — conversation messages with `partsJson` JSONB (added in migration 0002)
- `entitlement` — trial/paid plan tracking per user
- `modelUsage` — per-request AI cost records (tokens, cost, model, provider)

### Findings

| ID | Severity | Finding |
|---|---|---|
| D-01 | Medium | **No data retention policy.** Session records, messages, and model usage logs accumulate indefinitely. No scheduled cleanup job exists. |
| D-02 | High | **No GDPR/CCPA data deletion flow.** There is no "delete my account" endpoint or documented data erasure procedure. The `privacy/page.tsx` page exists but its content must match actual ability to fulfill deletion requests. |
| D-03 | Medium | **`partsJson` stores arbitrary `UIMessagePart[]` JSONB.** There is no schema validation on write; malformed or maliciously large payloads could corrupt the message history. Max size limits are not enforced at the DB layer. |
| D-04 | Low | **`modelUsage` table lacks a foreign key to `user`.** Cost attribution queries must join manually. Adding `userId` as an indexed foreign key would enable per-user cost dashboards. |
| D-05 | Low | **Chat sharing uses a boolean flag (`isPublic`) with no expiry or token.** Shared links never expire and cannot be revoked on a per-link basis — only by changing the flag. |

### Recommendations

- **D-R01 (Owner: Backend | Priority: High | Effort: 1d):** Implement a `/api/me/delete` endpoint that cascades deletion across `user`, `chat`, `message`, `session`, `entitlement`, `modelUsage`. Add a confirmation UI in settings.
- **D-R02 (Owner: Backend | Priority: Medium | Effort: 4h):** Add a Drizzle-scheduled cleanup query (or a cron webhook on Vercel) to purge sessions expired > 30 days and model usage records > 6 months.
- **D-R03 (Owner: Backend | Priority: Medium | Effort: 2h):** Add a `CHECK` constraint or Zod validation before `saveMessage()` that enforces a maximum `partsJson` size.

---

## Domain 5 — AI/ML & Model Governance

### Findings

| ID | Severity | Finding |
|---|---|---|
| M-01 | High | **No systematic prompt injection defense.** User messages and web search results flow into system prompts with string interpolation. Web search snippets in particular could contain adversarial instructions. `src/lib/web-search.ts` truncates to 600 chars but does not sanitize for embedded directives. |
| M-02 | Medium | **Builder prompt is 200+ lines and grows with each iteration.** The two-pass builder constructs large composite prompts. No token budget guard exists to prevent runaway context consumption for `nextjs-bundle` targets, which could inflate costs unexpectedly. |
| M-03 | Medium | **`qualityRetryThreshold` controls two-pass retries but is uncapped.** A pathological retry loop (e.g., perpetually bad quality score) could send unbounded API calls per user request. |
| M-04 | Low | **Model IDs are strings (`"gemini-2.5-flash"`) embedded in code.** If Google deprecates a model ID, the only signal is a runtime API error. No model availability check or graceful version negotiation exists. |
| M-05 | Low | **No output filtering pipeline.** Model responses are streamed directly to the client. No PII redaction, toxicity filter, or content policy check exists post-generation. |
| M-06 | Low | **System prompts for killers contain execution capability claims** (e.g., Code Wizard: "You have access to a run_code tool") that are conditionally false when `QUILL_SANDBOX_CONTAINER_ENABLED` is not set. This is documented in TODOS but unresolved. |

### Recommendations

- **M-R01 (Owner: Backend | Priority: High | Effort: 4h):** Sanitize web search results before interpolation into prompts. Strip or escape sequences matching `[system]`, `[user]`, `<|im_start|>`, `<|endoftext|>`, and common jailbreak prefixes.
- **M-R02 (Owner: Backend | Priority: Medium | Effort: 3h):** Add a `maxTokenBudget` guard in `two-pass-builder.ts` that aborts and returns the draft artifact if the context window would exceed a configurable threshold (e.g., 80K tokens for flash, 200K for pro).
- **M-R03 (Owner: Backend | Priority: Medium | Effort: 2h):** Cap `qualityRetryThreshold` to a maximum of 2 regardless of what the config passes in. Add a warning log when the cap is hit.

---

## Domain 6 — Reliability, Scalability & SLOs

### Findings

| ID | Severity | Finding |
|---|---|---|
| R-01 | High | **No defined SLO targets.** There are no documented availability, latency, or error rate targets. No alerting thresholds. Referenced as pending in `TODOS.md` but unactioned. |
| R-02 | High | **No external uptime monitoring.** `/api/health` with readiness probes is deployed and well-designed but not monitored by any external service. Downtime is only detectable by users. |
| R-03 | Medium | **Vercel function cold starts under load.** Neon serverless connections pool inconsistently under burst traffic. The `@neondatabase/serverless` driver mitigates this for HTTP connections but edge pool exhaustion is not handled explicitly. |
| R-04 | Medium | **In-memory rate limit fallback breaks horizontally.** If Upstash is unavailable, rate limits are enforced per-instance. On Vercel edge, multiple function instances enforce separate counters, allowing effective rate-limit bypass under load. |
| R-05 | Low | **No disaster recovery or backup procedure documented.** Neon provides point-in-time recovery at the infrastructure level, but there is no documented RPO/RTO, no tested restore procedure, and no backup verification schedule. |

### Recommendations

- **R-R01 (Owner: Infra | Priority: High | Effort: 2h):** Register all four key routes (`/`, `/agent`, `/api/health`, `/api/chat`) with Better Stack or UptimeRobot (free tier available). Define alert routing (email or webhook) before next production push.
- **R-R02 (Owner: Tech Lead | Priority: High | Effort: 2h):** Document initial SLO targets (e.g., 99.5% availability, p95 chat response < 8s) in `HEARTBEAT.md` and reference them from the deployment checklist.
- **R-R03 (Owner: Backend | Priority: Medium | Effort: 1d):** Document Neon PITR configuration, test a restore, and record the steps in `DEV_RUNBOOK.md`.

---

## Domain 7 — Testing, QA & Release Engineering

### Findings

| ID | Severity | Finding |
|---|---|---|
| T-01 | High | **No automated test suite.** There are no unit tests, integration tests, or E2E tests. `scripts/test-chatbot-responses.mjs` and `scripts/test-agent-remediation.mjs` are post-build smoke scripts, not regression tests. |
| T-02 | High | **No visible CI/CD pipeline** in the workspace (no `.github/workflows/`). All quality gates (`typecheck`, `lint`, `build`) are manual — they rely on developer discipline. |
| T-03 | Medium | **Bundle budget check (`scripts/check-bundle-budget.mjs`) is not enforced in a gate.** It exists as a script but is not in the deployment checklist as a required step. |
| T-04 | Medium | **No rollback procedure.** The deployment checklist describes smoke tests but no documented rollback path (e.g., Vercel instant rollback to prior deployment). |
| T-05 | Low | **`npm run lint` uses a flat ESLint config (`eslint.config.mjs`) but no strict rules list is visible.** The quality of lint enforcement is unclear. |

### Recommendations

- **T-R01 (Owner: Tech Lead | Priority: High | Effort: 3d):** Create a GitHub Actions workflow with at minimum: `npm ci` → `npm run typecheck` → `npm run lint` → `npm run build` → `node scripts/test-agent-remediation.mjs`. Gate all PRs on green.
- **T-R02 (Owner: Backend | Priority: High | Effort: 2d):** Add Vitest unit tests for the three highest-risk modules: `src/lib/rate-limit.ts`, `src/lib/entitlements.ts`, and `src/lib/builder-artifacts.ts` (parser). Target 80% branch coverage on these files.
- **T-R03 (Owner: Backend | Priority: Medium | Effort: 1d):** Add bundle budget enforcement (`npm run bundle:check`) to the CI pipeline with a documented per-route JS budget baseline.

---

## Domain 8 — Dependencies, Licensing & Cost Controls

### Findings

| ID | Severity | Finding |
|---|---|---|
| Dep-01 | Low | No high-risk licenses identified. All major dependencies are MIT or Apache 2.0. `better-auth` is MIT. Needs formal SBOM scan before commercial distribution. |
| Dep-02 | Medium | `@ai-sdk/*` packages are at v3.x and `ai` at v6.x. Major version drift between SDK packages is unusual and warrants review of compatibility assumptions. |
| Dep-03 | Low | `esbuild` is pinned via overrides to 0.27.5 to work around a build-kit conflict. This is a technical debt marker that should be revisited when upgrading the build stack. |
| Dep-04 | Medium | **No cost cap or budget alert on Google AI or Tavily APIs.** Runaway usage (e.g., automated scraping of the chat endpoint) could incur unbounded API costs. Rate limits provide some protection but no hard monetary cap exists. |

### Recommendations

- **Dep-R01 (Owner: Infra | Priority: Medium | Effort: 2h):** Set a Google Cloud billing alert at $50/month and $100/month thresholds. Set a Tavily usage alert at 80% of the monthly plan quota.
- **Dep-R02 (Owner: Tech Lead | Priority: Low | Effort: 2h):** Run `npm audit` and document the current vulnerability baseline. Add `npm audit --audit-level=high` to the CI pipeline. Produce a formal SBOM with `npm sbom` before any commercial launch.

---

## Domain 9 — Regulatory, Legal & AI Ethics

### Findings

| ID | Severity | Finding |
|---|---|---|
| Leg-01 | High | **Privacy policy coverage vs. data reality gap.** The `/privacy/page.tsx` page exists but there is no documented GDPR Article 17 (right to erasure) fulfillment path, no data processing register, and no DPA (Data Processing Agreement) process for EU users. |
| Leg-02 | High | **No Terms of Service enforcement.** `/terms/page.tsx` exists but there is no click-through acceptance flow at signup that timestamps agreement. Without a recorded ToS acceptance, enforcement is legally weakened. |
| Leg-03 | Medium | **AI-generated content disclosure.** Content generated by LLMs is served to users without systematic disclosure. EU AI Act obligations (for general-purpose AI systems) may require disclosure depending on deployment scope. |
| Leg-04 | Medium | **Sandbox false-claim risk.** System prompts claim execution capability that is disabled by default. If a user relies on claimed execution and suffers harm (data loss, incorrect computation), this could constitute misrepresentation. |
| Leg-05 | Low | **MIT license is public but the product is described as `private: true` in `package.json`.** Verify whether open-source licensing intentions are aligned with commercial model. |

### Recommendations

- **Leg-R01 (Owner: Product/Legal | Priority: High | Effort: 1d):** Add ToS acceptance checkbox with timestamp to the registration flow before commercial launch.
- **Leg-R02 (Owner: Product | Priority: High | Effort: 4h):** Implement `/api/me/delete` (see D-R01) and add it to the privacy policy as the documented erasure path.
- **Leg-R03 (Owner: Product | Priority: Medium | Effort: 2h):** Resolve TODOS.md item `Remove sandbox false claims OR implement real code executor`. Choose Option A (remove false claims) as the fastest path.

---

## Domain 10 — Holistic Risk Assessment

| Risk | Severity | Likelihood | Rating | Evidence |
|---|---|---|---|---|
| No revenue mechanism (no Stripe) | Critical | Certain | **Critical** | TODOS.md Priority 1 |
| Production Docker executor incompatibility | High | High (on Vercel) | **High** | docker-executor.ts: spawn() |
| Single AI provider failure (Gemini) | High | Low-Medium | **High** | model-selection.ts |
| No CI/CD pipeline | High | Certain | **High** | No .github/workflows visible |
| Ephemeral metrics loss | Medium | Certain | **Medium** | api-metrics.ts: globalThis |
| No uptime monitoring | High | Certain | **High** | TODOS backlog |
| GDPR erasure gap | High | Low-Medium (pre-EU scale) | **Medium** | No /me/delete endpoint |
| Prompt injection via web search | High | Low | **Medium** | web-search.ts sanitization |
| No automated tests | Medium | Certain | **Medium** | No test/ directory |
| OAuth / social login missing | Medium | Certain | **Medium** | TODOS Priority 1 |

---

## Prioritized Implementation Roadmap

### Immediate (0–2 weeks) — Revenue & Safety

| # | Action | Owner | Effort | Impact | Status |
|---|---|---|---|---|---|
| 1 | Implement Stripe checkout + webhook + plan activation | Full-stack | 5d | Business-critical | Pending |
| 2 | Resolve sandbox false claims (TODOS Option A: remove execution language from Code Wizard prompt when disabled) | Backend | 2h | Legal/trust | ✅ **DONE** (Apr 12) |
| 3 | Deploy external uptime monitoring (Better Stack free tier) | Infra | 1h | Reliability | Pending |
| 4 | Implement `/api/csp-report` violation logging | Backend | 3h | Security | Pending |
| 5 | Add `npm audit` gate to deployment checklist | DevOps | 30m | Security | Pending |

### Short-Term (2–6 weeks) — Auth, Data Rights & CI

| # | Action | Owner | Effort | Impact |
|---|---|---|---|---|
| 6 | Add Google OAuth via Better Auth | Full-stack | 1d | Growth/UX |
| 7 | Implement `/api/me/delete` account erasure | Backend | 1d | GDPR/legal |
| 8 | Create GitHub Actions CI pipeline | DevOps | 3h | Quality |
| 9 | Add Vitest unit tests for rate-limit, entitlements, builder parser | Backend | 2d | Quality |
| 10 | Add ToS acceptance timestamp to registration | Full-stack | 3h | Legal |
| 11 | Replace in-memory metrics with persistent sink | Backend | 1d | Observability |

### Medium-Term (6–12 weeks) — AI Resilience & Scalability

| # | Action | Owner | Effort | Impact |
|---|---|---|---|---|
| 12 | Add AI provider failover (Anthropic/OpenAI for Think/Pro fallback) | Backend | 3d | Reliability |
| 13 | Sanitize web search snippets against prompt injection | Backend | 4h | Security |
| 14 | Replace client-side Babel with server-side transpile for React preview | Frontend | 2d | Performance |
| 15 | Define and document SLO targets; add alert thresholds | Tech Lead | 2h | Reliability |
| 16 | Add token budget guard in two-pass builder | Backend | 3h | Cost control |
| 17 | Document and test Neon PITR restore procedure | Infra | 4h | DR |

### Long-Term (3–6 months) — Platform Maturity

| # | Action | Owner | Effort | Impact |
|---|---|---|---|---|
| 18 | Builder Phase 8–11 (one-click export, dependency guardrails, deploy pipeline) | Full-stack | ongoing | Product differentiation |
| 19 | PWA service worker + offline shell | Frontend | 3d | Mobile UX |
| 20 | Replace exec-in-code with cloud sandbox (E2B / Modal) | Backend | 5d | Feature completeness |
| 21 | Formal SBOM + license compliance review | Legal/DevOps | 1d | Commercial readiness |

---

## Risk Mitigation Plan

| Risk | Mitigation | Deadline |
|---|---|---|
| No Stripe | Block v1.1 release gate on Stripe integration | Week 2 |
| Docker executor on Vercel | Add build-time assertion that rejects deployment if `QUILL_SANDBOX_CONTAINER_ENABLED=true` on non-Docker host | Week 1 |
| Gemini outage | Configure `THINKING_MODEL_OVERRIDE` to a secondary provider as documented fallback | Week 6 |
| Prompt injection | Sanitize all third-party content before interpolation into prompts | Week 4 |
| GDPR erasure | Implement and document delete endpoint before EU user acquisition | Week 4 |

---

## Traceability Matrix

| Finding ID | Recommendation ID | Roadmap Item | Deliverable |
|---|---|---|---|
| A-03 | A-R03 | #2 | Sandbox false claims resolved / executor replaced |
| S-01 | S-R01 | #4 | CSP violation logging endpoint |
| S-03 | S-R02 | #1 | Stripe replaces env-var entitlements |
| D-02 | D-R01 | #7 | Account erasure endpoint + privacy policy update |
| T-01/T-02 | T-R01/T-R02 | #8/#9 | CI pipeline + Vitest test suite |
| R-01/R-02 | R-R01/R-R02 | #3/#15 | Uptime monitoring + SLO documentation |
| M-01 | M-R01 | #13 | Web search prompt injection sanitization |
| Leg-01/02 | Leg-R01/02 | #7/#10 | ToS acceptance + GDPR erasure |
| G-01 | G-R01 | — | ADR directory |

---

## Data Privacy & AI Ethics Checklist

- [ ] Right to erasure (`/api/me/delete`) — **MISSING**
- [ ] ToS click-through with timestamp — **MISSING**
- [ ] AI-generated content disclosure — **MISSING**
- [ ] Data retention and automatic deletion policy — **MISSING**
- [x] Session PII (IP, user agent) stored with cascading deletion on user delete
- [x] HTTPS enforced (HSTS 1yr)
- [x] Password hashing delegated to Better Auth (bcrypt)
- [x] Rate limiting to prevent abuse
- [x] Guest mode with limited access
- [ ] Prompt injection defense on external content — **PARTIAL** (truncation only)
- [ ] Human-in-the-loop for high-autonomy (checkpointed-auto) operations — **UI concept exists, not enforced server-side**

---

## Technical Appendix

### Key Environment Variable Risk Surface

| Variable | Risk If Missing | Risk If Leaked |
|---|---|---|
| `BETTER_AUTH_SECRET` | Auth broken (crash on start) | Session forgery |
| `DATABASE_URL` | App nonfunctional | Full DB access |
| `GOOGLE_GENERATIVE_AI_API_KEY` | AI features down | Unlimited API charges |
| `UPSTASH_REDIS_REST_TOKEN` | Rate limits degrade to per-instance | Rate limit bypass |
| `API_METRICS_TOKEN` | Metrics endpoint disabled | Internal telemetry exposure |
| `PAID_USER_EMAILS` / `PAID_USER_IDS` | No prod paid access | Entitlement bypass |
| `QUILL_SANDBOX_CONTAINER_ENABLED` | Code execution disabled | Unintended exec on incompatible host |

### Builder Artifact Maturity

| Target | Status | Gaps |
|---|---|---|
| `page` | Production-ready | None |
| `react-app` | Beta (client Babel risk) | Server-side transpile |
| `document` | Functional | No export format |
| `nextjs-bundle` | Alpha | Local-only validation, no deploy path |

### Database Index Coverage

| Table | Indexed columns | Gaps |
|---|---|---|
| `session` | `userId` | `expiresAt` for cleanup queries |
| `account` | `userId` | — |
| `chat` | `userId`, `createdAt` | — |
| `message` | `chatId` | None identified |
| `entitlement` | `userId` | — |
| `modelUsage` | `userId`, `createdAt` | `provider`, `modelId` for analytics |

---

## What Is Needed to Complete the Audit

The following information could not be assessed from the repository alone:

| Missing Artifact | Why Needed | Collection Step |
|---|---|---|
| CI/CD pipeline config (`.github/workflows/`) | Release engineering, change failure rate | Check GitHub repo → Actions tab |
| Vercel deployment configuration | Environment variable hygiene, edge/serverless split | Review Vercel dashboard |
| Production error log sample | Real incident patterns, top-5 errors | Export 7-day error log from Vercel |
| `npm audit` output | Dependency vulnerability baseline | Run `npm audit --json > audit.json` |
| Bundle analysis report | JS budget baseline, vendor chunk sizes | Run `npm run analyze` |
| Neon database size + slow query log | Scaling risk, index gaps | Pull from Neon dashboard |
| Stakeholder interviews (if team > 1) | Roadmap alignment, ownership clarity | 30-min structured interview using G-R01 ADR template |

---

**Next Steps:** Review the roadmap prioritization with your team. Start with items #1–5 (Revenue & Safety immediate blockers), then proceed to #6–11 (Auth & Compliance short-term). See main project `TODOS.md` for detailed acceptance criteria per roadmap item.
